# Raw wget mirror - intermediate input to build.py, not part of the site.
mirror/

# Playwright MCP scratch output
.playwright-mcp/

.DS_Store
__pycache__/

# The QPT specification stays out of the published site AND out of the public
# repo. Committing it here would make it readable at github.com/... even though
# nothing on the site links to it. Remove this line if you want it public.
qpt-*.md
